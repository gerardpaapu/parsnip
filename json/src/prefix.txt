Parser.Json = (function () {
    'use strict';
    var modules = {
        '../lib/Parser': Parser
    };
    function require(name) {
        if (!(name in modules)) {
            throw new Error('Module not found: ' + name);
        }
        return modules[name];
    }

