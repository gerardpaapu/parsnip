Parsnip.Json = (function () {
    'use strict';
    var modules = {
        '../lib/Parsnip': Parsnip
    };
    function require(name) {
        if (!(name in modules)) {
            throw new Error('Module not found: ' + name);
        }
        return modules[name];
    }

